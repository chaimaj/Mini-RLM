******** Mini RLM *********

windows/cygwin
Netbeans

1- Run the RLM_Client (main.cpp)
2- Run the RLM_Router (main.cpp)
3- Run the RLM_Application (main.cpp)

* Use the file starwars.mov provided (Readflie reads only a specific type of files) make sure to put the right location.
in the server's main function.
* If you are not running on the same machine make sure to change the addresses in the router main function.
